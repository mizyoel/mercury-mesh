# Mercury Mesh: ignore runtime state (logs, inbox, sessions)
.mesh/orchestration-log/
.mesh/log/
.mesh/decisions/inbox/
.mesh/sessions/
# Mercury Mesh: local workstream activation file (local to this machine)
.mesh-workstream
