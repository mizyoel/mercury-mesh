OPENROUTER_API_KEY=<set-openrouter-api-key>
# Optional alias if you want the nervous system to read a mesh-specific key name.
MESH_EMBEDDING_KEY=<optional-mesh-embedding-key>

# Required for GitHub issue triage and workflow automation.
GITHUB_TOKEN=<set-github-token>